# DS_BeRightBack
The data science project repository.

TODO: add your name and student id to notepad file

## Setup Instructions
In the terminal/command line
**1. Create a virtual environment**

python -m venv venv

**2. Activate the virtual environment**

venv\Scripts\activate

**3. Install dependencies**

pip install -r requirements.txt

Then, in your IDE or Jupyter:

Select kernel → Python (venv)

**Git Rules** 

Do not commit:

venv/

or any large raw data files